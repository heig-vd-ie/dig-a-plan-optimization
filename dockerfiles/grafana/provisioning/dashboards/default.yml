
apiVersion: 1

providers:
  - name: Ray    # Default dashboards provided by OSS Ray
    folder: Ray
    type: file
    options:
      path: /var/lib/grafana/dashboards
  - name: Experiments
    folder: Experiments
    type: file
    options:
      path: /var/lib/grafana/dashboards-experiments